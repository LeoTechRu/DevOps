# Phonebook

Каноническая версия консольного phonebook-lab, выделенная из legacy `studyPython/13`.

Почему именно этот вариант:

- структура уже разбита на `controller/`, `model/`, `view/`;
- есть `test_phonebook.py`;
- это более полная версия по сравнению с ранними `7/` и `11/`.

Старые версии не удалены, а сложены в `python/archive/study-python-misc/`.
