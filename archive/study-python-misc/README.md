# Study Python Misc Archive

Архив хвостов из legacy `studyPython`, которые не стали отдельными курсами или lab'ами.

Что лежит здесь:

- `7/` и `11/` — ранние версии phonebook и учебных упражнений;
- `Solutions/` — разрозненные решения и шпаргалки;
- `blog_project/` — отдельная Django-заготовка;
- `homework_04/` — ранний async/API-кейс;
- `result.txt` и старые корневые файлы — технические и исторические хвосты.

Назначение каталога:

- не терять историю старой песочницы;
- не держать эти материалы в корне `python/`;
- иметь отдельное место для всего, что пока не стоит превращать в самостоятельный `lab`.
